# numeraire

```{eval-rst}
.. automodule:: numeraire
   :no-members:
```

The top-level namespace re-exports the common surface; see {doc}`index` for the grouped summary.
Each object is documented under its home module (linked from the pages listed there).
